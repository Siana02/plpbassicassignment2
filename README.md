# plpbassicassignment2
## Steps and Commands Used

### Local Setup

1. **Create a New Folder**:
   ```bash
    mkdir
     plpbassicassignment2 
         cd plpbassicassignment2 
         initialize a git repository 
         git init
         create a README.md file 
         echo "# PLPBasicGitAssignment" > README.md
         add and commit the README.md file 
         git add README.md
         git commit -m "Initial commit with README.md"
         create a remote repository on github and add the remote respository
         git remote add origin https://github.com/your-username/PLPBasicGitAssignment.git
         push the initial commit to github 
         git branch -M main
         git push -u origin main
         create and commit hello.txt 
         echo "Hello, Git!" > hello.txt
         git add hello.txt
         git commit -m "Add hello.txt with a welcome message"
         git push origin main